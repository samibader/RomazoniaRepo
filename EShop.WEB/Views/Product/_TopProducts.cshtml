@model List<EShop.BLL.DTO.ProductDTO>


<div class="best-sale-product">
    <div class="sidebar-bar-title">
        <h3>@ViewBag.Title</h3>
    </div>
    <div class="block-content">
        <div class="slider-items-products">
            <div id="best-sale-slider" class="product-flexslider hidden-buttons">
                <div class="slider-items slider-width-col4">
                    <div class="product-item">
                        @{ var cnt = Math.Min(Model.Count, 4);}
                        @for (int i = 0; i < cnt; i++)
                        {

                            <div class="best-sale-item">
                                <div class="products-block-left">
                                    <a href="@Url.Action("Browse", "Product", new {id = Model[i].SKUId})" title="@Model[i].Name" class="product-image"><img src="@(Model[i].Images[0]).ashx?height=100" alt="@Model[i].Name"></a>
                                </div>
                                <div class="products-block-right">
                                    <p class="product-name">
                                        <a href="@Url.Action("Browse", "Product", new {id = Model[i].SKUId})">@Model[i].Name</a>
                                    </p>
                                    <span class="price">@Model[i].TotalPriceDisplay</span>

                                    <div class="rating">
                                        @{Html.RenderPartial("_Stars", Model[i].TotalRate);}
                                    </div>
                                  
                                </div>
                            </div>
                        }
                    </div>
                    @*<div class="product-item">
                        @for (int i = 4; i < 8; i++)
                        {

                            <div class="best-sale-item">
                                <div class="products-block-left">
                                    <a href="@Url.Action("Browse","Product",new {id=Model[i].SKUId})" title="@Model[i].Name" class="product-image"><img src="~/uploads/@(Model[i].Images[0]).ashx?height=150" alt="@Model[i].Name"></a>
                                </div>
                                <div class="products-block-right">
                                    <p class="product-name">
                                        <a href="@Url.Action("Browse","Product",new {id=Model[i].SKUId})">@Model[i].Name</a>
                                    </p>
                                    <span class="price">@Model[i].TotalPriceDisplay</span>

                                    @{Html.RenderPartial("_Stars", Model[i].TotalRate);}
                                </div>
                            </div>
                        }
                    </div>*@
                </div>
            </div>
        </div>
    </div>
</div>

